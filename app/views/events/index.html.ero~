<h1>Home - Private Events</h1>

<% if user_signed_in? %>
	<div>
		<%= current_user.email %> <br> <br>
		<%= link_to "Log out", destroy_user_session_path, 'data-turbo-method': :delete %>
	</div>
	<br>
	<div>
	<% if notice %>
		<div>
			<%= notice %>
		</div>
	<% end %>
	</div>
<% else %>
	<div>
		<%= link_to "Login here", new_user_session_path %><br>
		Don't have an account? Sign in <%= link_to "here", new_user_registration_path %>
		<br>
		<br>
	</div>
<% end %>

<div>
	<p>
		There is <%= @counting_events %> events in here!
	</p>
</div>

<br>

	<div>
		<% unless @events_upcoming.empty? %>
			<h4>Upcoming Events</h4>
			<% @events_upcoming.each do |event| %>
				<% if event.privated? %>
					<% if event.attendeer && event.creator == current_user %>
						Author : <%= event.creator.email %><br>
						Event Name : <%= link_to event.event_name, event %><br>
						Date : <%= event.date %><br>
						Location : <%= event.location %><br>
						Participants : <%= event.attendeer.email %><br>
					<% end %>
				<hr>
				<% else %>
					Author : <%= event.creator.email %><br>
					Event Name : <%= link_to event.event_name, event %><br>
					Date : <%= event.date %><br>
					Location : <%= event.location %><br>
					<hr>
				<% end %>
			<% end %>
			
		<% else %>
			<h3>There is no upcoming events. Let's make some event!</h3>
		<% end %>

		<hr>

		
		<% unless @events_past.empty? %>
			<h4>Past Events</h4>
			<% @events_past.each do |event| %>
				<% unless event.privated? %>
					Author : <%= event.creator.email %><br>
					Event Name : <%= link_to event.event_name, event %><br>
					Date : <%= event.date %><br>
					Location : <%= event.location %><br>
					<hr>
				<% end %>
			<% end %>
		<% else %>
			<h3>There is no past events.</h3>
		<% end %>
	</div>
<%= link_to "Add new event", new_event_path %>
